[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecg_lab_core"
version = "0.1.0"

[tool.setuptools]
# 显式指定你要打包的文件夹名
packages = ["ecg_core"]
